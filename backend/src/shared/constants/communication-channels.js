const CONVERSATION_SOURCES = Object.freeze({
  PORTAL: "PORTAL",
  EMAIL: "EMAIL",
  API: "API",
  WHATSAPP: "WHATSAPP",
  SMS: "SMS"
});

const ACTIVE_CONVERSATION_SOURCES = Object.freeze([
  CONVERSATION_SOURCES.PORTAL,
  CONVERSATION_SOURCES.EMAIL,
  CONVERSATION_SOURCES.API
]);

const RESERVED_CONVERSATION_SOURCES = Object.freeze([
  CONVERSATION_SOURCES.WHATSAPP,
  CONVERSATION_SOURCES.SMS
]);

const EMAIL_DIRECTION = Object.freeze({
  INBOUND: "INBOUND",
  OUTBOUND: "OUTBOUND"
});

module.exports = {
  CONVERSATION_SOURCES,
  ACTIVE_CONVERSATION_SOURCES,
  RESERVED_CONVERSATION_SOURCES,
  EMAIL_DIRECTION
};
